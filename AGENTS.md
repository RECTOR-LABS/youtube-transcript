<!-- Satellite context file — extends the global hub (~/.claude/CLAUDE.md | ~/.pi/agent/AGENTS.md). Host-neutral; project-specific only. Do not duplicate hub standards here. -->

# youtube-transcript

> A small Python utility to fetch YouTube video transcripts.

## Stack

Python · `youtube_transcript.py` (single-script entry).

## Common Commands

```bash
python youtube_transcript.py <video-id-or-url>
```

## Notes

- Single-file utility — no package metadata, no tests. Treat as a standalone helper script, not a project.